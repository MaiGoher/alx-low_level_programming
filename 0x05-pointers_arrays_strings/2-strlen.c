#include "main.h"
#include <stdio.h>

/**
 * int _strlen - a function that returns the length of a string
 * @s: input
 * Return: str
 */
 int _strlen(char *s)
{
int str;
str = sizeof(*s);
return str;
	
}
