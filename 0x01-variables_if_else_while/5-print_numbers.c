#include <stdio.h>

/**
 * main - print numbers of base 10
 * Return: 0
 */

int main(void)
{
int count = 0;
while (count <= 0 && count >= 9)
{
printf("%d", count);
count += 1;
}
printf("/n");
return (0);
}
