#include "main.h"

/**
 * int _islower(int c) - return 1 is lowercase
 * retrun 0 otherwise
 * Return: nothing.
 */

int _islower(int c)
{
if(c <= 'z' || c >= 'a')
{
return (1);
}
else
{
return (0);
}
}
